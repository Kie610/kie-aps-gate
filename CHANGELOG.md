# Changelog

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従う。

## [Unreleased]

### Removed

- 実験フラグ 2 本 (`immobilizeClonePhysBones` / `freezeClonePbWhileMoving`) を削除した。
  未リリースの実験機能で、実機 A/B の結果どちらも不採用になった (下記の調査記録を参照)。

### 調査記録: 固定体の揺れものが本体の移動・回転を慣性として拾う問題 (2026-08-23)

対策は載せない判断になったが、機序と実測結論は再発見コストが高いためここへ残す。

**症状**: 体固定 + PB 固定解除の状態で、固定体 (分身) の髪などの揺れものが、
本体 (操作しているゴースト) の移動・回転・急停止に反応して流れる・暴れる。

**機序 (コード読解 + 実測で確定)**:
APS の体固定は「分身を作る」のではなく、**実ボーンの駆動元を constraint で世界固定の
fix 骨格へ切り替える**構造 (固定体 = 実メッシュ + 実 PhysBone である APS_PB 複製。
歩き続ける方がゴーストのプロキシ体)。APS_WorldFix 配下の PhysBone 73 個はすべて
ポーズ操作用グラブハンドルで、揺れもの複製ではない。アバタールート (ゴースト) が
動き続けるため、PhysBone のアバター空間シミュレーションが本体の移動・回転を
世界固定ボーンへ慣性として注入する。

**試して駄目だった対策**:

1. **Immobile World / 1.0 への切替 — 不発 (実測で確定)**。対象 (APS_PB 複製) と
   レイヤー配管が正しいことを確認した状態での不発なので、「Immobile World は
   世界固定ボーンへの慣性注入を打ち消さない」が結論。公式文の
   "positional movement" の打ち消しは世界空間での root 移動が前提で、世界固定
   ボーンには打ち消す対象が無く、慣性はシミュレーション空間 (アバター空間) 自体の
   移動から注入される。**ワールド固定小物の定石 (Immobile World) がこのケースへ
   通用しない理由もこれ。**
2. **移動・回転中だけ固定体の PB を凍結 — 動作はするが UX 上不採用**。
   実機で「歩き出して止まる・立ち止まると動く」までは確認できたが、
   (a) 動き出した瞬間の慣性は凍結が間に合わず入ってしまう、
   (b) 「移動を全くせず体の部位だけ動かす」場面が実用上まれで、凍結の代償に
   見合わない — の 2 点でユーザー判断により不採用。

**残る道**: VRChat SDK へのフィードバック (最小再現: 素のアバター +
MA World Fixed Object の箱 + 髪チェーン PB 1 本、その場回転で流れる) /
ExtraBone ワークフロー (揺らしたいチェーンへ APS:ExtraBone を付けると
ポーズ可能なハンドル化され、ライブシミュレーションから外れるため症状も出ない)。

## [0.5.0-alpha]

「ポーズを固定した瞬間に揺れものがレスト状態になる」を、設定なしで直す回。

### Changed

- **揺れ物の「その場の姿勢」での固定を、ゲート有効時の既定動作にした。**
  これまでは `揺れ物固定をその場の姿勢で行う` チェック (アバター全体が対象) を
  入れたときだけ効き、コンポーネントを置いていないアバター
  (`Tools > kieApsGate > プロジェクト全体で有効化` だけで使う場合) では
  **黙って無効**だった。
  - 原因の機序: APS はアバターの全 PhysBone を `APS_PB` という複製へ移し、
    固定時にその複製を非アクティブにする。`Reset When Disabled` が ON の複製は
    **切られた瞬間にレスト位置へ戻ってから固まる** (ミルフィでは 39 個中 19 個が ON)。
  - 本バージョンからは、**APS が固定で切り替える複製に限って** `Reset When Disabled`
    を自動でオフにする。固定した瞬間の形で固まり、解除するとその姿勢から物理が
    再開する (レスト位置へは戻らない)。他ギミックの PhysBone には触らないので、
    リセット前提のギミックと衝突しない
  - `揺れ物固定をその場の姿勢で行う` は「アバター全体 (他ギミック含む) へ広げる」
    オプションとして残る。フィールド名・既定値は変更なし
- ゲートした constraint の**パスをログへ出す**ようにした (これまでは件数のみで、
  何が止まったのかをあとから確かめられなかった)

### Known limitations

- APS が複製名 `APS_PB` や切り替え方法を変えた場合、この強制は効かなくなる
  (警告を出して素通しする。アバターは壊さない)

## [0.4.0-alpha]

0.3.0-alpha で失った効果を、`Reset When Disabled` を使って正しく取り戻す回。
**どちらの設定も既定はオフ。0.3.0-alpha から上げても挙動は変わらない。**

### Added

- **`PhysBone を含むサブツリーもゲートする`**（コンポーネントのチェックボックス）。
  ゲート対象内の PhysBone の `Reset When Disabled` を強制的にオフにしたうえで
  ゲートする。オフの PhysBone は復帰時に現在の姿勢を捨てないので、0.2.0-alpha の
  「固定した瞬間に揺れものがレスト位置で固まる」不具合を起こさずに落とせる。
  - 実測でゲートできる constraint が **10 個 → 53 個**に戻った（ミルフィ本番構成）。
- **`揺れ物固定をその場の姿勢で行う`**（コンポーネントのチェックボックス）。
  アバター全体の PhysBone の `Reset When Disabled` をオフにする。
  APS の揺れ物固定が、レスト位置へ戻らず**その場の姿勢**で固まるようになる。
  - これは kieApsGate のゲートとは無関係な、APS 側の挙動の修正。
    ミルフィ本番構成では 49 個中 19 個の PhysBone が `Reset When Disabled` オンで、
    **揺れ物を固定した瞬間に変位の約 7 割を失っていた**（Play モードで実測）。
  - PhysBone がリセットされる前提の他ギミックがある場合はオフのままにすること。

### 検証

Unity 2022.3.22f1 の Play モードで、NDMF フルビルド後のアバターの
`APS_FixBody` / `APS_FixPB` を直接駆動して計測した（ミルフィ本番構成、PhysBone 49 個・
観測ボーン 179 本）。

| | 素の APS | 本バージョン（両方オン） |
|---|---|---|
| ゲートできた constraint | 10 個 | **53 個** |
| 揺れ物固定での姿勢保持 | 変位の約 7 割を喪失 | **100% 保持**（固定中のドリフト 0.0°）|
| 揺れ物固定の解除 | 正常 | 正常 |
| 体固定／解除 | 正常 | 正常 |

### Known limitations

- **体固定中に見えている「固定体」の髪の見た目は目視確認できていない。**
  APS の体固定は実ボーンを constraint で拘束せず、固定体は fix 骨格＋複製メッシュで、
  その揺れ物は実髪をミラーせず fix 骨格側のハンドル PhysBone が独自に
  シミュレートする構造のため、数値で判定できなかった。
  理屈のうえでは `Reset When Disabled` の強制で解消しているはずだが、
  `PhysBone を含むサブツリーもゲートする` を使う前に一度 in-game で確認することを勧める。

## [0.3.0-alpha]

### 修正

- **固定した瞬間に PhysBone がレスト位置で固まる不具合を直した。**
  非アクティブにした PhysBone は、戻された瞬間に現在の姿勢を捨ててレスト位置から
  初期化される。0.2.0-alpha までは PhysBone を含むサブツリーまで落としていたため、
  固定するとツインテール等がレスト位置へスナップしていた
  （報告: 頭を斜めにしてツインテールをたわませた状態で固定して再現）。
  - クローン骨格（`WorldFix`）を**安全チェックを通さずに一括で**落としていた。
    実測でこのサブツリーには PhysBone が 73 個あった。
  - 個別 constraint 側のチェックは `enabled` な PhysBone しか弾いていなかった。
    APS のクローン骨格の PhysBone は編集時に無効で、固定時にアニメーションで
    有効化されるため素通りしていた。**`enabled` を見ない判定に変えた。**

### 効果への影響（重要）

- ゲートできる constraint が実測で **48 個 → 10 個**に減った。
  **0.2.0-alpha までの -1.83 ms は成立しない。数値は再計測待ち。**
  正しさを優先した結果であり、意図した変更。

## [0.2.0-alpha]

### 破壊的変更

- **既定をオフにした。入れただけでは何もしない。**
  0.1.0-alpha は、入れるとプロジェクト内の APS 入りアバター全部へ一律にかかった。
  有効化の方法は下記 2 つ。

### Added

- アバター単位で有効・無効を切り替えるコンポーネント `kieApsGate`
  (`Add Component > Kie > kieApsGate`)。
- `Tools > kieApsGate > プロジェクト全体で有効化` でプロジェクト内を一括有効化。
  コンポーネントが付いていればそちらが優先されるので、一括有効化したうえで
  特定のアバターだけ除外できる。

### 確認

- **APS 4.2.7 で動作することを確認した**（0.1.0-alpha は 4.3.14 でのみ確認）。
  本番構成のアバターで候補 58 件 → 50 個をゲート。

## [0.1.0-alpha] - 2026-08-12

初回リリース。

### Added

- AvatarPoseSystem のトラッキング用 constraint を、ポーズ未固定中は
  GameObject ごと非アクティブにする NDMF プラグイン。
  `APS_FixBody` が立つと復帰する。
- 対象の特定は APS の生成クリップから引く（`m_Enabled` を 1 にも 0 にも
  されている constraint = クローン追従専用）。名前に依存しないので
  APS の内部名が変わっても追従する。
- クローン骨格サブツリー（`FixRoot` を子に持つオブジェクト）も併せてゲート。
- `Tools > APS Gate > Enabled` で一時的に無効化できる。

### 実測

ミルフィ素体 + ギミック7種で **-1.83 ms/frame**（12.80 → 10.97）。
APS を丸ごと削除した場合の -2.83 ms に対して約 65% を回収。

### Known limitations

- アバター個別の無効化が無い。プロジェクト単位で一律にかかる（0.2.0-alpha で解消）。
- APS 自身が `m_Enabled` を管理している constraint（実測で 56 個中 8 個相当）は
  対象外。これらも止めるには Prepare の 2〜3 フレームだけ再有効化する制御が要り、
  窓を外すとポーズがズレる／固定後に流れるため、意図的に見送っている。
  取りこぼしは VRCConstraintJob 換算で約 0.83 ms。
